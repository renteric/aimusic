yt-dlp>=2026.01.01
click>=8.1.7
mutagen

# FastAPI stack
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# JWT auth
python-jose[cryptography]>=3.3.0

# Password hashing (kept for backward compat with existing werkzeug hashes)
werkzeug>=3.1.0

# HTTP client (stem proxy)
httpx>=0.27.0

# Anthropic Claude SDK (AI intelligence layer)
anthropic>=0.40.0

# Melody extraction (librosa + pYIN pitch tracking, MIDI, MusicXML)
librosa>=0.10.0
soundfile>=0.12.0
mido>=1.3.0
music21>=9.3.0
numpy>=1.24.0
scipy>=1.11.0
tqdm>=4.65.0

# Settings / env
python-dotenv>=1.0.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
flake8

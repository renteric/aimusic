# Core ML
demucs>=4.0.0
openunmix>=1.3.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0
numpy>=1.26.0
scipy>=1.12.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0
httpx>=0.27.0

# AudioSep Python source is cloned from GitHub in the Dockerfile builder stage.
# The checkpoint (~1 GB) is bind-mounted from ./models/separator/audiosep/ — run `make download-separator`.

# Utilities
tqdm>=4.66.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
rich>=13.0.0
click>=8.1.0
python-dotenv>=1.0.0
